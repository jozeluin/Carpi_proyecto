export const Usuario = ({ nombre, edad, nacionalidad }) => {
  return (
    <div>
      <h1>Nombre: {nombre}</h1>
      <p>Edad: {edad}</p>
      <p>Nacionalidad: {nacionalidad}</p>
      <hr />
    </div>
  );
};
